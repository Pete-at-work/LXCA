---
- hosts: host-win
  gather_facts: false

  tasks:
  - name: Get info for all installed services
    ansible.windows.win_service_info:
    register: service_info.display_name

#  - name: print to screen
#    debug:
#      msg: "{{display_name.service_info}}"
